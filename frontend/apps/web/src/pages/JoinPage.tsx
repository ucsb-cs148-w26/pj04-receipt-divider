// TODO: implement join flow
// - read group ID from URL search params (?id=abc123)
// - GET /api/group/join?id=abc123 (auth handled server-side)
// - on success, navigate to /group/:roomId
// - handle errors

export default function JoinPage() {
  return <p>JoinPage — WIP</p>;
}
