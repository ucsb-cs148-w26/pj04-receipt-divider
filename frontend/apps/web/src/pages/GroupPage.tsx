// TODO: implement group page
// - read roomId from route params (/group/:roomId)
// - fetch group/receipt data from backend
// - render receipt items and participants
// - handle errors

export default function GroupPage() {
  return <p>GroupPage — WIP</p>;
}
